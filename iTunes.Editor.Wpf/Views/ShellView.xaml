<UserControl x:Class="ITunes.Editor.Views.ShellView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ITunes.Editor.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">
    
    <ContentControl x:Name="Current" />

    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="This is the main text" />
        <Button Grid.Row="1" x:Name="LoadITunes" Content="Load iTunes" />
        <ContentControl x:Name="Songs" Content="ContentControl" Grid.Column="1" Grid.RowSpan="2"/>
    </Grid>-->
</UserControl>
